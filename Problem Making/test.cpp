#include "testlib.h"
#include <bits/stdc++.h>
#define pb push_back
using namespace std;

int main(int argc, char* argv[]) {
  registerGen(argc, argv, 1);
  int l = atoi(argv[1]);
  int r = atoi(argv[2]);
  int z = atoi(argv[3]);
  cout << l << ' ' << r << ' ' << z << endl; 
}
