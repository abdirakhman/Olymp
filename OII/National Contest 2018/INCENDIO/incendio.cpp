#include <stdio.h>
int used[2222][2222];
int timer = 1;
bool in(int x, int y) {
	
} 
void dfs(int x, int y, int t) {
	if (!in(x + t, y) && !in(x - t, y) && !in(x, y - t) && !in(x, y + t)) {
		
	}		
}
int alzati(int N, int M, int X[], int Y[]) {
	for (int i = 0; i < M; i++) {
	}
	int l = 1, r = N;
	while(r - l > 1) {
		int m = (l + r) / 2;
		cnt ++;
		if (f(m)) {
			/
		} else {
			/
		}		
	}	
}

